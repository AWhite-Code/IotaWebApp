@model IEnumerable<IotaWebApp.Models.WebsiteContent>

@{
    ViewData["Title"] = "Home";

    Func<string, string> GetContentValue = key => Model.FirstOrDefault(c => c.ContentKey == key)?.ContentValue ?? string.Empty;

    var heroImage = GetContentValue("HeroImage") ?? "/assets/placeholder.png";
    var heroTitle = GetContentValue("HeroTitle") ?? "Default Hero Title";
    var heroButtonText = GetContentValue("HeroButtonText") ?? "Learn More";
    var heroButtonLink = GetContentValue("HeroButtonLink") ?? "#";

    var section1Title = GetContentValue("Section1Title") ?? "Section Title";
    var section1Description = GetContentValue("Section1Description") ?? "Section description...";
    var section1ButtonText = GetContentValue("Section1ButtonText") ?? "Click Here";
    var section1ButtonLink = GetContentValue("Section1ButtonLink") ?? "#";

    var circleIconsImage = GetContentValue("CircleIconsImage") ?? "/assets/circle-icons.png";

    var placeholderImage1 = GetContentValue("CarouselPlaceholder1") ?? "/assets/placeholder.png";
    var placeholderImage2 = GetContentValue("CarouselPlaceholder2") ?? "/assets/placeholder.png";
    var placeholderImage3 = GetContentValue("CarouselPlaceholder3") ?? "/assets/placeholder.png";

    var sectionTitle = "Explore our latest work...";

    var leftImage = GetContentValue("LeftColumnImage") ?? "/assets/placeholder.png";
    var leftTitle = GetContentValue("LeftColumnTitle") ?? "Case Study Name Here";
    var leftBody = GetContentValue("LeftColumnBody") ?? "Lorem ipsum dolor...";
    var leftLink = GetContentValue("LeftColumnLink") ?? "#";

    var middleImage = GetContentValue("MiddleColumnImage") ?? "/assets/placeholder.png";
    var middleTitle = GetContentValue("MiddleColumnTitle") ?? "Case Study Name Here";
    var middleLink = GetContentValue("MiddleColumnLink") ?? "#";

    var rightImage = GetContentValue("RightColumnImage") ?? "/assets/placeholder.png";
    var rightTitle = GetContentValue("RightColumnTitle") ?? "Case Study Name Here";
    var rightLink = GetContentValue("RightColumnLink") ?? "#";

    var iconOffice = GetContentValue("IconOffice") ?? "/assets/Group 456.png";
    var iconEducation = GetContentValue("IconEducation") ?? "/assets/Group 457.png";
    var iconHealthcare = GetContentValue("IconHealthcare") ?? "/assets/Group 458.png";
    var iconHospitality = GetContentValue("IconHospitality") ?? "/assets/Group 459.png";

    var discoverTitle = GetContentValue("DiscoverTitle") ?? "Discover more about iOTA";
    var discoverSubtitle = GetContentValue("DiscoverSubtitle") ?? "Find out who we are, why we do it and why we love what we do.";
    var buttonTealText = GetContentValue("ButtonTealText") ?? "WATCH THE VIDEO";
    var buttonMustardText = GetContentValue("ButtonMustardText") ?? "ABOUT iOTA";
    var backgroundImage = GetContentValue("BackgroundImage") ?? "/assets/image1.png";
}

<div class="grid-container fluid full">
    <div class="grid-x">
        <div class="cell large-12 medium-12 small-12">
            <div class="carousel">
                <div>
                    <img src="@Url.Content(heroImage)" alt="Hero Image" class="hero-image" />
                    <div class="carousel-caption">
                        <h1>@heroTitle</h1>
                    </div>
                </div>

                @{
                    var carouselSlides = Model.Where(c => c.ContentKey.StartsWith("CarouselSlide"));
                    foreach (var slide in carouselSlides)
                    {
                        <div>
                            <img src="@Url.Content(slide.ContentValue ?? "/assets/placeholder.png")" alt="@slide.ContentKey" class="hero-image" />
                        </div>
                    }

                    var placeholderImages = new List<string> { placeholderImage1, placeholderImage2, placeholderImage3 };
                    foreach (var placeholderImage in placeholderImages)
                    {
                        <div>
                            <img src="@Url.Content(placeholderImage)" alt="Placeholder Image" class="hero-image" />
                        </div>
                    }
                }
            </div>

            <div class="carousel-button-container text-center">
                <a href="@heroButtonLink" class="button">@heroButtonText</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('.carousel').slick({
                dots: true,
                infinite: true,
                speed: 500,
                fade: true,
                cssEase: 'linear',
                autoplay: true,
                autoplaySpeed: 3000
            });
        });
    </script>
}

<div class="full-screen-light-grey">
    <div class="grid-container full-height">
        <div class="grid-x grid-margin-x align-center full-height">
            <div class="cell medium-6 small-12 align-center text-center">
                <img src="@Url.Content(circleIconsImage)" alt="Circle Icons" class="circle-icons">
            </div>

            <div class="cell medium-6 small-12 align-center">
                <div class="text-section">
                    <h2>@section1Title</h2>
                    <p>@section1Description</p>
                    <a href="@section1ButtonLink" class="mustard-button">@section1ButtonText</a>
                </div>
            </div>
        </div>
    </div>
</div>

<h2 class="section-title text-center">@sectionTitle</h2>

<div class="grid-container">
    <div class="grid-x grid-margin-x align-center">
        <div class="cell medium-4 small-12">
            <div class="content-box">
                <div class="image-overlay-container float-right">
                    <img src="@Url.Content(leftImage)" alt="@leftTitle" class="case-study-image">
                    <div class="overlay-content left">
                        <h3>@leftTitle</h3>
                        <p>@leftBody</p>
                        <a href="@leftLink" class="view-more-link">View More → <i class="fa fa-arrow-right"></i></a>
                    </div>
                </div>
            </div>
        </div>
        <div class="cell medium-4 small-12 text-center">
            <div class="content-box">
                <div class="image-overlay-container">
                    <img src="@Url.Content(middleImage)" alt="@middleTitle" class="case-study-image">
                    <div class="overlay-content bottom">
                        <h3>@middleTitle</h3>
                        <a href="@middleLink" class="view-more-link">View More → <i class="fa fa-arrow-right"></i></a>
                    </div>
                </div>
            </div>
        </div>
        <div class="cell medium-4 small-12">
            <div class="content-box">
                <div class="image-overlay-container float-left">
                    <img src="@Url.Content(rightImage)" alt="@rightTitle" class="case-study-image">
                    <div class="overlay-content bottom">
                        <h3>@rightTitle</h3>
                        <a href="@rightLink" class="view-more-link">View More → <i class="fa fa-arrow-right"></i></a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="dark-blue-band">
    <div class="grid-container">
        <div class="grid-x align-center">
            <div class="cell medium-2 small-0"></div> 
            <div class="cell medium-2 small-6 text-center margin-left-3 margin-right-3">
                <img src="@Url.Content(iconOffice)" alt="Office" class="icon-image" />
            </div>

            <div class="cell medium-2 small-6 text-center margin-left-3 margin-right-3">
                <img src="@Url.Content(iconEducation)" alt="Education" class="icon-image" />
            </div>

            <div class="cell medium-2 small-6 text-center margin-left-3 margin-right-3">
                <img src="@Url.Content(iconHealthcare)" alt="Healthcare" class="icon-image" />
            </div>

            <div class="cell medium-2 small-6 text-center margin-left-3 margin-right-3">
                <img src="@Url.Content(iconHospitality)" alt="Hospitality" class="icon-image" />
            </div>
            <div class="cell medium-2 small-0"></div> 
        </div>
    </div>
</div>

<div class="full-width-image-section">
    <img src="@Url.Content(backgroundImage)" alt="Background Image" class="full-width-background-image" />

    <div class="grid-container full">
        <div class="grid-x align-center align-middle full-height">
            <div class="cell large-8 medium-10 small-12 text-center">
                <h2 class="discover-title">@discoverTitle</h2>
                <p class="discover-subtitle">@discoverSubtitle</p>
                <div class="button-group">
                    <a href="#" class="button teal-button">@buttonTealText</a>
                    <a href="#" class="button mustard-button">@buttonMustardText</a>
                </div>
            </div>
        </div>
    </div>
</div>